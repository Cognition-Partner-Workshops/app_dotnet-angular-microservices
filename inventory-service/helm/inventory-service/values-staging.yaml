replicaCount: 2

image:
  tag: "staging-latest"

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 75

ingress:
  hosts:
    - host: inventory-staging.local
      paths:
        - path: /
          pathType: Prefix

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: inventory-service-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Cognition-Partner-Workshops/ordermanager-microservices.git
    targetRevision: main
    path: inventory-service/helm/inventory-service
    helm:
      valueFiles:
        - values.yaml
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: decomposition-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

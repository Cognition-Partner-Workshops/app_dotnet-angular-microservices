replicaCount: 2

image:
  tag: "staging-latest"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 75

ingress:
  enabled: true
  hosts:
    - host: inventory-staging.example.com
      paths:
        - path: /
          pathType: Prefix

replicaCount: 2
image:
  tag: staging-latest
ingress:
  hosts:
    - host: inventory-service-staging.workshop.local
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 75
